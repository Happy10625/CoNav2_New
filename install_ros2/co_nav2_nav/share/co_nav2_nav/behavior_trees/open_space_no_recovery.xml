<!--
  Minimal navigation tree for a supervised empty-area test.  A planning or
  control failure propagates directly to the caller: there is deliberately no
  Spin, BackUp, costmap-clearing, or other autonomous recovery movement.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <PipelineSequence name="NavigateWithoutRecovery">
      <RateController hz="1.0">
        <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
      </RateController>
      <FollowPath path="{path}" controller_id="FollowPath"/>
    </PipelineSequence>
  </BehaviorTree>
</root>
